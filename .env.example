# ===========================================
# Vercel デプロイ時に設定が必要な環境変数
# ===========================================

# --- 必須 ---

# PostgreSQL 接続URL (Vercel Postgres / Supabase / Neon 等)
# 例: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=

# JWT署名用シークレットキー (32文字以上のランダム文字列を推奨)
# 生成例: openssl rand -base64 32
JWT_SECRET=

# --- オプション ---

# JWTトークンの有効期限 (デフォルト: 24h)
# JWT_EXPIRES_IN=24h
